## 2023-10-25 - Accessible Command Palette
**Learning:** Custom command palettes (Hyper-WIMP) often omit explicit `<label>` tags for aesthetic reasons, making filter inputs invisible to screen readers without `aria-label`.
**Action:** Always add explicit `aria-label` attributes to custom search inputs and action buttons in 'HUD' or modal-style overlays.
